# Test Branch

This branch is created to test the `create_pull_request` tool functionality.

## Tool being tested
- `create_pull_request` - Creates GitHub Pull Requests via API

## Test scenario
1. Create a new branch
2. Add a commit to ensure there are changes
3. Create a PR using the new tool
4. Verify the PR is created successfully